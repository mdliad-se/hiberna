<?xml version="1.0" encoding="utf-8"?>
<!-- Jinatra Brand Guidelines v1.1 adaptive icon background layer - the
     Night Indigo (#2F2BD1) full-bleed fill copied in from the sibling
     brand/ directory's android/ic_launcher_background.svg (not referenced at
     build time - copied so this repo never depends on a path outside it),
     converted to VectorDrawable syntax (same fill, same full-canvas rect). -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#2F2BD1"
        android:pathData="M0,0h108v108h-108z" />
</vector>
