# .spine/config.md — pinned cross-cutting context (asserted before task one)
- Stack:            Kotlin + Jetpack Compose (Material3). AGP 8.9.0-rc01+, Gradle KTS, kotlinx.serialization. compileSdk/targetSdk 37 (Android 17), minSdk 30 (Android 11). Android only — no desktop, no iOS. Privilege via Shizuku (`dev.rikka.shizuku:api`), root via libsu deferred to a later version.
- Test runner:      JUnit4 + Robolectric (JVM) + Compose UI tests via `createComposeRule()`. Turbine for flows. Every privileged call sits behind a `ShellBackend` interface with a `FakeShellBackend`; one instrumented smoke test covers the real Shizuku path on device.
- DB / project / env: No database. DataStore Preferences + kotlinx.serialization JSON for settings, presets and exclude-overrides; snapshots as individual JSON files under `filesDir/snapshots/`. Verification device: **Pixel 10 Pro, Android 17 (API 37), Shizuku activated via wireless debugging** — confirmed working 2026-09-02. Package: `com.jinatra.hiberna`.
- Auth context:     No user accounts, no network identity. The only privilege source is the Shizuku binder service; absence of it is a first-class UI state, never an error dialog.
- Conventions:      Clean reimplementation — NOT a fork. `no_more_background/` is a behavioural reference only: no copied Kotlin/Dart, no copied i18n strings. Policy is keyed on **package name**, with uid resolved fresh from `PackageManager` at apply time (upstream keys on uid, which rots after reinstall). Brutalist UI per Jinatra Brand Guidelines v1.1 — 3px Ink borders, hard offset shadows, radius 0 — except the app icon, which stays flat per v1.1 §14. Product colour Night Indigo `#2F2BD1`.
- Boot assertion:   On startup in a release build, assert the resolved `ShellBackend` is the real Shizuku implementation and `error()` loudly if a fake or stub is wired in. The failure this app cannot afford is a build that appears to restrict apps while touching nothing.
